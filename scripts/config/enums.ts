export const enums = {
    subscriptionStatus: ['active', 'cancelled', 'expired'] as string[],
    pageType: ['marketing', 'property', 'admin'] as string[],
    domainStatus: ['pending', 'active', 'failed'] as string[]
}; 